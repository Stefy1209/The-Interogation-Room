OPENAI_API_KEY=sk-your-key-here

# true = use canned mock replies/claims instead of calling OpenAI (default,
# lets you build/demo the full loop with zero API spend). Set to false once
# real chat + extraction calls are wired in and tested.
USE_MOCK=true
